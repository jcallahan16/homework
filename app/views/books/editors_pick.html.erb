<h1>Editor's Pick</h1>
 

  <% editors_pick = Book.where("title" => "A Game of Thrones") %>
  <% editors_pick = Book.first %>

  <%= link_to (editors_pick["title"]), "/books/#{editors_pick["id"]}" %> 
